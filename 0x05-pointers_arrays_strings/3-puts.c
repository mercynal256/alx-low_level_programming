#include "main.h"

/**
 * _puts - prints a string
 * @str: the string
 *
 * Return: the length of the string
 */
void _puts(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		{
			if(str[i] != ' ' && str[i] != '\t' && str[i] != '\n')				{
			index = i
			}
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
